#include <stddef.h>
    /*
    compute: dequeue task, work on task -> return back to balancer node
    balancer: req from client -> task queue -> get finished task and return to client

    client: -> balancer
    */

int dsm_main(char * mem_region, size_t length, int argc, char * argv[]) 
{
    
}